import { MarketingClient } from "./MarketingClient";

export const metadata = { title: "Marketing & Notifications — Weego Admin" };

export default function MarketingPage() {
  return <MarketingClient />;
}
